ريدك أن تجعل هذا المشروع الـ Full Stack (واجهة أمامية + Backend) جاهزًا بالكامل للنشر على Render، وذلك عبر تنفيذ الخطوات التالية داخل ملفات المشروع:

📂 1️⃣ هيكلة المشروع وبناء الواجهة

تأكد من أن المشروع يحتوي على مجلدين رئيسيين:

/frontend ← واجهة React أو Vue

/backend ← سيرفر Node.js (Express)

في مجلد frontend:

نفّذ npm install

نفّذ npm run build لبناء الواجهة الأمامية.

انسخ محتوى مجلد build (في حالة React) أو dist (في حالة Vue) إلى:

/backend/public


إذا لم يكن مجلد public موجودًا داخل backend، أنشئه أولًا.

🧭 2️⃣ تعديل سيرفر الـ Backend

في ملف backend/server.js أو backend/index.js:

استورد الموديولات التالية:

import express from "express";
import path from "path";
import { fileURLToPath } from "url";


أضف هذا الكود أسفل تهيئة السيرفر:

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const app = express();
const PORT = process.env.PORT || 10000;

// خدمة الملفات الثابتة من مجلد public
app.use(express.static(path.join(__dirname, "public")));

// أي Route غير معروف يرجّع index.html (لدعم React/Vue Router)
app.get("*", (req, res) => {
  res.sendFile(path.join(__dirname, "public", "index.html"));
});

app.listen(PORT, () => {
  console.log(`✅ Server is running on port ${PORT}`);
});


تأكد من حذف أي مسارات كانت ترجع أخطاء ENOENT أو تحاول الوصول إلى ملفات غير موجودة.

⚙️ 3️⃣ إعداد ملف Render

أنشئ ملفًا باسم render.yaml في جذر المشروع أو تأكد من إعداد الخدمة على النحو التالي:

services:
  - type: web
    name: dental-fullstack
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production


أو عند النشر يدويًا في لوحة Render:

Root Directory = backend

Build Command = npm install

Start Command = npm start

🧪 4️⃣ التحقق محليًا

شغّل npm start من داخل مجلد backend.

افتح http://localhost:10000 وتأكد أن الواجهة تعمل بشكل صحيح.

🚀 5️⃣ النتيجة النهائية

المشروع يصبح Full Stack جاهز للنشر على Render دون خطأ ENOENT.

عند فتح رابط الخدمة الأساسية، تظهر الواجهة الأمامية.

الـ API يعمل من نفس الدومين.

المسارات غير المعروفة يتم توجيهها تلقائيًا إلى index.html لدعم React/Vue Router.

✅ الهدف: تنفيذ جميع التعديلات السابقة تلقائيًا داخل المشروع الحالي على Replit بحيث يصبح جاهزًا للـ Deploy على Render مباشرة دون تدخل يدوي.